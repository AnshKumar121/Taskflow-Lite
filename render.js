export function renderTaskList(el,tasks){el.innerHTML='';if(!tasks.length){el.innerHTML='<li>No tasks yet</li>';return;}tasks.forEach(task=>{const li=document.createElement('li');li.className='task '+(task.completed?'completed':'');li.dataset.id=task.id;li.innerHTML=`<label><input type='checkbox' ${task.completed?'checked':''}><span>${task.text}</span></label><button class='delete-btn'>Delete</button>`;el.appendChild(li);});}